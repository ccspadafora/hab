from django.apps import AppConfig


class AuthPropietariosConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.auth_propietarios'
    label = 'auth_propietarios'
    verbose_name = 'Auth portal — Propietarios'
