from django.apps import AppConfig


class AuthConstructorasConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.auth_constructoras'
    label = 'auth_constructoras'
    verbose_name = 'Auth portal — Constructoras'
