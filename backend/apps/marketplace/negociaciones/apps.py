from django.apps import AppConfig


class NegociacionesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.marketplace.negociaciones'
    label = 'marketplace_negociaciones'
    verbose_name = 'Marketplace — Negociaciones'
